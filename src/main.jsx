// src/index.jsx
import React from 'react'
import ReactDOM from 'react-dom'
import './index.css'  // Import your Tailwind CSS file here
import App from './App'
// help to highlight potential problem, additional  check and issue (deprecated, not supported api component and api)

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
